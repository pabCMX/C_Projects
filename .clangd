# Portable flags for all machines. System headers come from the local GCC via
# --query-driver in .vscode/settings.json and .zed/settings.json (not hardcoded -I paths).
CompileFlags:
  Add:
    - -std=c23
    - -Wall
    - -Wextra
    - -Wpedantic
  Compiler: gcc
